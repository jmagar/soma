{
  "name": "rtemplate-mcp",
  "description": "TEMPLATE: Replace with a one-line description of your MCP server. E.g.: 'Gotify notification server MCP bridge for Gemini CLI.'",
  "author": "Jacob Magar",
  "repository": "https://github.com/jmagar/rtemplate-mcp",
  "license": "MIT",
  "keywords": [
    "example",
    "mcp",
    "template",
    "homelab"
  ],
  "contextFileName": "GEMINI.md",
  "mcpServers": {
    "rtemplate": {
      "type": "stdio",
      "command": "${extensionPath}${/}bin${/}example",
      "args": [
        "mcp"
      ],
      "env": {
        "RTEMPLATE_API_URL": "${settings.rtemplate_api_url}",
        "RTEMPLATE_API_KEY": "${settings.rtemplate_api_key}",
        "RUST_LOG": "warn"
      }
    }
  },
  "skills": "./skills",
  "hooks": "./hooks/hooks.json",
  "settings": [
    {
      "name": "server_url",
      "description": "Optional HTTP server base URL for remote/platform fallback and health monitoring. The default MCP connection uses local stdio instead.",
      "required": false
    },
    {
      "name": "api_token",
      "description": "Optional bearer token for HTTP MCP fallback. Not used by the default stdio connection.",
      "required": false,
      "sensitive": true
    },
    {
      "name": "rtemplate_api_url",
      "description": "TEMPLATE: Replace 'rtemplate_api_url' with your service's credential field name. For platform servers, this is the deployed REST API base URL used by the local stdio adapter. For upstream-client servers, this is the upstream service endpoint. Maps to RTEMPLATE_API_URL env var.",
      "required": false
    },
    {
      "name": "rtemplate_api_key",
      "description": "TEMPLATE: Replace 'rtemplate_api_key' with your service's credential field name. API key or bearer token for your upstream service. Maps to RTEMPLATE_API_KEY env var.",
      "required": false,
      "sensitive": true
    }
  ]
}
