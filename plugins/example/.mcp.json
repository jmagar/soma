{
  "mcpServers": {
    "example": {
      "type": "stdio",
      "command": "${CLAUDE_PLUGIN_ROOT}/bin/rtemplate",
      "args": ["mcp"],
      "env": {
        "EXAMPLE_API_URL": "${user_config.example_api_url}",
        "EXAMPLE_API_KEY": "${user_config.example_api_key}",
        "RUST_LOG": "warn"
      }
    }
  }
}
