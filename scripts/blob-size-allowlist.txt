# Repo-relative paths or glob patterns allowed to exceed the default blob limit.
# Keep this list short. Prefer Git LFS or generated artifacts outside git.
#
# Examples:
# bin/example
# docs/references/**/*.md

# Vendored JSON Schema (660 definitions) codex-app-server-client's build.rs
# reads directly to generate protocol types via typify - a real, necessary
# checked-in asset (the crate can't build without it), not something to
# shrink or keep out of git. Regenerate via `cargo xtask codex-schema regen`.
crates/codex-app-server-client/schema/protocol.schema.json
