{
  "schema_version": 1,
  "provider": {
    "name": "hello-static",
    "kind": "static-rust",
    "title": "Hello Static",
    "version": "0.1.0"
  },
  "tools": [
    {
      "name": "hello_static",
      "description": "Static manifest example action.",
      "input_schema": {
        "type": "object",
        "properties": {
          "message": { "type": "string" }
        },
        "additionalProperties": false
      },
      "output_schema": {
        "type": "object",
        "additionalProperties": true
      },
      "cli": {
        "enabled": true,
        "command": "hello_static",
        "about": "Run the static provider example."
      },
      "rest": {
        "enabled": true,
        "method": "POST",
        "path": "/v1/providers/hello_static",
        "summary": "Run the static provider example."
      }
    }
  ]
}
