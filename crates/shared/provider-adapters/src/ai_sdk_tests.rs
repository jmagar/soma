// Structural sibling for ai_sdk.rs. Executable sidecar behavior needs a real
// `node` binary and is covered end-to-end by Soma's
// `apps/soma/tests/ai_sdk_provider.rs`, which drives this crate's
// `AiSdkProvider` through crates/soma/application's drop-in provider
// directory loader.
