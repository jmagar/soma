// Structural sibling for openapi.rs. Executable OpenAPI provider behavior
// (network calls, host allowlisting, capability enforcement) is covered
// end-to-end by Soma's `apps/soma/tests/openapi_provider.rs`, which
// drives this crate's `OpenApiProvider` directly.
