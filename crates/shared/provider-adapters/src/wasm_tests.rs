// Structural sibling for wasm.rs. Executable WASM behavior needs a compiled
// fixture module and is covered end-to-end by Soma's
// `apps/soma/tests/wasm_provider.rs` and `drop_provider_probe.rs`, which
// drive this crate's `WasmProvider` through crates/soma/application's
// drop-in provider directory loader.
