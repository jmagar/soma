{
  "name": "codex-app-server-rest-client",
  "version": "0.0.0",
  "private": true,
  "description": "Generated TypeScript types + a thin hand-written fetch wrapper for the codex-app-server-client REST adapter's OpenAPI spec. Proves the REST API is consumable outside Rust.",
  "type": "module",
  "engines": {
    "node": ">=20.19.0"
  },
  "scripts": {
    "generate": "node scripts/generate.mjs",
    "check-sync": "node scripts/generate.mjs --check",
    "typecheck": "tsc --noEmit",
    "test": "node --test",
    "smoke": "node examples/smoke.ts",
    "live-smoke": "node scripts/live-smoke.mjs"
  },
  "devDependencies": {
    "@types/node": "^24.0.0",
    "openapi-typescript": "^7.13.0",
    "typescript": "^6.0.3"
  }
}
