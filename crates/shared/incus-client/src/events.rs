//! Filled in by Task 4 (WebSocket events subscription, behind the `events`
//! feature). Stubbed here in Task 1 so `cargo fmt`'s module resolution
//! (which does not evaluate `#[cfg(feature = ...)]` when locating `mod`
//! declarations) can resolve this file for the whole plan, matching the
//! same-task stubbing pattern already used for `transport.rs`,
//! `operations.rs`, and `resources.rs`.
