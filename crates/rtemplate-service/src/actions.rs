//! Compatibility re-exports for the template action contract.
//!
//! The canonical action metadata lives in `rtemplate-contracts`; this module is
//! kept so template structure checks and older scaffold docs still have the
//! service-layer action entry point they expect.

pub use rtemplate_contracts::actions::*;
