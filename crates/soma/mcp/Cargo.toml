[package]
name = "soma-mcp"
version = "0.4.1"
edition = "2021"
rust-version = "1.96"
authors.workspace = true
description = "MCP surface for soma"
homepage.workspace = true
license = "MIT"
repository.workspace = true
publish = false

[package.metadata.soma-architecture]
layer = "product-surface"

[features]
default = []
stdio = ["rmcp/transport-io"]
http = ["dep:axum", "rmcp/transport-streamable-http-server"]
auth = ["dep:soma-auth"]
oauth = ["auth"]
test-support = []

[dependencies]
anyhow = "1"
axum = { version = "0.8", optional = true }
http = "1"
rmcp = { version = "=2.2.0", default-features = false, features = [
  "server",
  "macros",
  "elicitation",
  "schemars",
] }
rmcp-traces = { path = "../../shared/traces" }
soma-auth = { path = "../../shared/auth", optional = true, features = ["http-axum"] }
soma-application = { path = "../application", default-features = false }
soma-contracts = { path = "../contracts" }
soma-domain = { path = "../domain" }
soma-mcp-server = { path = "../../shared/mcp/server" }
schemars = "1.2.1"
serde = { version = "1", features = ["derive"] }
serde_json = "1"
tokio = { version = "1", features = ["io-std", "macros"] }
tracing = "0.1"
url = "2"

[dev-dependencies]
async-trait = "0.1"
rmcp = { version = "=2.2.0", default-features = false, features = [
  "client",
  "transport-io",
] }
soma-test-support = { path = "../test-support" }
tempfile = "3"
tokio = { version = "1", features = ["io-util", "macros", "rt"] }
tracing-subscriber = { version = "0.3", features = ["fmt"] }
