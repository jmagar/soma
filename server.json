{
  "$schema": "https://static.modelcontextprotocol.io/schemas/2025-12-11/server.schema.json",
  "description": "Drop-in RMCP runtime for provider-backed MCP tools, prompts, resources, and agent capabilities.",
  "name": "dinglebear.ai/soma",
  "packages": [
    {
      "environmentVariables": [
        {
          "description": "Optional deployed platform API or upstream API base URL used by the SomaClient compatibility layer. Leave unset for local provider/runtime mode.",
          "isRequired": false,
          "isSecret": false,
          "name": "SOMA_API_URL"
        },
        {
          "description": "Optional bearer token or upstream API key for provider-backed calls that use SomaClient. Keep secret.",
          "isRequired": false,
          "isSecret": true,
          "name": "SOMA_API_KEY"
        },
        {
          "description": "Optional static bearer token required when exposing the HTTP MCP transport in bearer-auth mode.",
          "isRequired": false,
          "isSecret": true,
          "name": "SOMA_MCP_TOKEN"
        },
        {
          "description": "Optional runtime data directory. Defaults to ~/.soma on bare metal and /data in containers.",
          "isRequired": false,
          "isSecret": false,
          "name": "SOMA_HOME"
        }
      ],
      "identifier": "ghcr.io/jmagar/soma:0.4.6",
      "registryType": "oci",
      "transport": {
        "type": "streamable-http",
        "url": "http://127.0.0.1:40060/mcp"
      },
      "version": "0.4.6"
    },
    {
      "environmentVariables": [
        {
          "description": "Optional absolute path to the installed soma binary. When unset, the npm launcher runs soma from PATH.",
          "isRequired": false,
          "isSecret": false,
          "name": "SOMA_BIN"
        },
        {
          "description": "Optional runtime data directory. Defaults to ~/.soma.",
          "isRequired": false,
          "isSecret": false,
          "name": "SOMA_HOME"
        },
        {
          "description": "Optional deployed platform API or upstream API base URL used by the SomaClient compatibility layer. Leave unset for local provider/runtime mode.",
          "isRequired": false,
          "isSecret": false,
          "name": "SOMA_API_URL"
        },
        {
          "description": "Optional bearer token or upstream API key for provider-backed calls that use SomaClient. Keep secret.",
          "isRequired": false,
          "isSecret": true,
          "name": "SOMA_API_KEY"
        }
      ],
      "identifier": "soma-rmcp",
      "registryType": "npm",
      "transport": {
        "type": "stdio"
      },
      "version": "0.4.6"
    }
  ],
  "repository": {
    "source": "github",
    "url": "https://github.com/jmagar/soma"
  },
  "title": "Soma",
  "version": "0.4.6",
  "websiteUrl": "https://github.com/jmagar/soma"
}
